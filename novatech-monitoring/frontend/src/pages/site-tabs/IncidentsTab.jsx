import IncidentsTable from '../../components/incidents/IncidentsTable.jsx'

/** Pestana "Incidencias": incidencias de la obra (seccion 31). */
export default function IncidentsTab({ siteId }) {
  return <IncidentsTable siteId={siteId} />
}
