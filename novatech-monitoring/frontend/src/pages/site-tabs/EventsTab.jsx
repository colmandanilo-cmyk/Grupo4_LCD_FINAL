import EventsTable from '../../components/events/EventsTable.jsx'

/** Pestana "Eventos": historial de la obra (seccion 27). */
export default function EventsTab({ siteId }) {
  return <EventsTable siteId={siteId} />
}
